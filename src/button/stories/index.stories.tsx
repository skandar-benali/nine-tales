import * as React from 'react';
import { Button as ButtonComponent } from '../component/button';

export default {
  title: 'Button',
  parameters: {
    info: { inline: true },
  },
};

export const Button = () => (
    <ButtonComponent  />
);
